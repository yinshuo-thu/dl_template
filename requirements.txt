# EDA 分析所需的基础包
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
seaborn>=0.12.0
scipy>=1.9.0
statsmodels>=0.13.0

# Jupyter Notebook 支持
jupyter>=1.0.0
ipykernel>=6.0.0
notebook>=6.0.0

# 自动化EDA工具（可选，根据需要安装）
# 注意：ydata-profiling 和 dataprep 有依赖冲突（pydantic版本要求不同）
# 建议只安装其中一个，或使用不同的虚拟环境
ydata-profiling>=4.0.0  # 原 pandas-profiling，推荐安装（需要 pydantic>=2）
sweetviz>=2.1.0  # 推荐安装
# dataprep>=0.4.0  # 已注释：与 ydata-profiling 冲突（需要 pydantic<2.0）
# 如需使用 dataprep，请注释掉 ydata-profiling 或使用单独的虚拟环境
autoviz>=0.1.0  # 可选

# 自动化特征工程工具（可选，根据需要安装）
featuretools>=1.0.0  # 自动化特征工程
tsfresh>=0.20.0  # 时间序列特征提取
autofeat>=2.0.0  # 自动特征工程

# AutoML工具（可选，根据需要安装）
autogluon>=0.8.0  # 推荐：功能强大的AutoML

